# 🚀 วิธีติดตั้ง Solana CLI - หลายทางเลือก

## ⚠️ ปัญหา

WSL กระพริบแล้วหายไป = WSL ยังไม่ได้ติดตั้ง

---

## ✅ วิธีที่ 1: ใช้ Git Bash (ง่ายที่สุด - ถ้ามี Git)

### ขั้นตอน:

1. **เปิด Git Bash** (หาใน Start Menu)

2. **รันคำสั่ง:**
   ```bash
   sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
   ```

3. **เลือก "stable" เมื่อถูกถาม**

4. **เพิ่ม PATH:**
   ```bash
   echo 'export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc
   ```

5. **ตรวจสอบ:**
   ```bash
   solana --version
   ```

**เสร็จแล้ว!** ✅

---

## ✅ วิธีที่ 2: ติดตั้ง WSL (ถ้าไม่มี Git Bash)

### ขั้นตอน:

1. **เปิด PowerShell เป็น Administrator:**
   - คลิกขวาที่ PowerShell
   - เลือก "Run as Administrator"

2. **รันคำสั่ง:**
   ```powershell
   wsl --install
   ```

3. **รีสตาร์ทคอมพิวเตอร์** (จำเป็น!)

4. **หลังรีสตาร์ท:**
   ```powershell
   wsl
   ```

5. **ตั้งค่า username และ password** (ครั้งแรก)

6. **ติดตั้ง Solana CLI:**
   ```bash
   sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
   ```

---

## ✅ วิธีที่ 3: ใช้ Windows Installer โดยตรง

### ขั้นตอน:

1. **ดาวน์โหลด installer:**
   - ไปที่: https://github.com/anza-xyz/agave/releases
   - ดาวน์โหลด: `agave-install-init-x86_64-pc-windows-msvc.exe`

2. **รัน installer พร้อมระบุ data-dir:**
   ```powershell
   .\agave-install-init-x86_64-pc-windows-msvc.exe --data-dir "$env:USERPROFILE\.local\share\solana\install" latest
   ```

3. **เพิ่ม PATH:**
   ```powershell
   $solanaPath = "$env:USERPROFILE\.local\share\solana\install\active_release\bin"
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";$solanaPath", [EnvironmentVariableTarget]::User)
   ```

4. **รีสตาร์ท PowerShell**

5. **ตรวจสอบ:**
   ```powershell
   solana --version
   ```

---

## 🎯 แนะนำ

**วิธีที่ง่ายที่สุด:**
1. **ถ้ามี Git Bash:** ใช้วิธีที่ 1 (ง่ายที่สุด!)
2. **ถ้าไม่มี:** ติดตั้ง WSL (วิธีที่ 2) หรือใช้ Windows Installer (วิธีที่ 3)

---

## 📝 ตรวจสอบว่ามี Git Bash หรือไม่

**ใน PowerShell:**
```powershell
where git
```

**ถ้าเจอ:** ใช้ Git Bash ได้เลย!

---

## ✅ สรุป

**วิธีที่ง่ายที่สุด:**
1. ใช้ Git Bash (ถ้ามี) ← **แนะนำ!**
2. หรือติดตั้ง WSL: `wsl --install` (ต้องรีสตาร์ท)
3. หรือใช้ Windows Installer (ต้องระบุ version)

**ลองวิธีไหนก่อนดี?** 🚀



