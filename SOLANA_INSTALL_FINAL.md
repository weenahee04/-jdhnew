# 🚀 ติดตั้ง Solana CLI - วิธีสุดท้าย (แนะนำ)

## ⚠️ ปัญหา

Windows installer มีปัญหา:
- ไม่รองรับ "stable" แล้ว
- ต้องระบุ version ที่ถูกต้อง
- Format version อาจไม่ตรง

---

## ✅ วิธีที่ง่ายที่สุด: ใช้ WSL

### ขั้นตอน:

1. **เปิด WSL:**
   ```powershell
   wsl
   ```

2. **รันคำสั่งติดตั้ง:**
   ```bash
   sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
   ```

3. **เลือก "stable" เมื่อถูกถาม**

4. **รอให้ติดตั้งเสร็จ** (~2-3 นาที)

5. **ตั้งค่า PATH:**
   ```bash
   echo 'export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc
   ```

6. **ตรวจสอบ:**
   ```bash
   solana --version
   ```

**เสร็จแล้ว!** ✅

---

## 📝 ใช้ Solana CLI ใน WSL

**ทุกครั้งที่ต้องการใช้:**
```powershell
wsl
solana --version
```

**หรือใช้จาก PowerShell:**
```powershell
wsl solana --version
```

---

## 🎯 หลังติดตั้งเสร็จ

```bash
# ใน WSL
solana config set --url devnet
solana airdrop 2
solana config get
```

---

## 💡 ทำไมใช้ WSL?

- ✅ ง่ายกว่า - ไม่ต้องระบุ version
- ✅ เสถียรกว่า - ลิงก์ใช้ได้แน่นอน
- ✅ ไม่มีปัญหา format version
- ✅ รองรับ "stable" channel

---

## 🔧 ถ้าต้องการใช้ Windows Installer

ลอง version ต่างๆ:
```powershell
# ลอง version ต่างๆ
.\solana-install-init-x86_64-pc-windows-msvc.exe 3.0.12
.\solana-install-init-x86_64-pc-windows-msvc.exe v3.0.12
.\solana-install-init-x86_64-pc-windows-msvc.exe latest
```

**แต่แนะนำให้ใช้ WSL ดีกว่า!**

---

## ✅ สรุป

**วิธีที่ง่ายที่สุด:**
1. เปิด WSL: `wsl`
2. รัน: `sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"`
3. เลือก "stable"
4. ตรวจสอบ: `solana --version`

**ใช้เวลา:** ~3-5 นาที

---

**ลองใช้ WSL ดูครับ!** 🚀




