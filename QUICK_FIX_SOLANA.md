# ⚡ แก้ไข Solana Installer Error - วิธีเร็วๆ

## ⚠️ ปัญหา

Windows installer มีปัญหา:
- ไม่รองรับ "stable" channel
- ต้องระบุ version ที่ถูกต้อง
- Format version ซับซ้อน

---

## ✅ วิธีแก้ที่ง่ายที่สุด: ใช้ WSL

### 3 ขั้นตอน:

1. **เปิด WSL:**
   ```powershell
   wsl
   ```

2. **รันคำสั่ง:**
   ```bash
   sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
   ```

3. **เลือก "stable" เมื่อถูกถาม**

**เสร็จแล้ว!** ✅

---

## 📝 ใช้ Solana CLI

**จาก PowerShell:**
```powershell
wsl solana --version
wsl solana config set --url devnet
wsl solana airdrop 2
```

**หรือเข้า WSL ก่อน:**
```powershell
wsl
# แล้วใช้คำสั่ง solana ตามปกติ
solana --version
```

---

## 🎯 หลังติดตั้งเสร็จ

```bash
# ใน WSL
solana config set --url devnet
solana airdrop 2
solana config get
```

---

## 💡 ทำไมใช้ WSL?

- ✅ ง่ายกว่า - ไม่ต้องระบุ version
- ✅ เสถียรกว่า - ลิงก์ใช้ได้แน่นอน
- ✅ ไม่มีปัญหา format
- ✅ รองรับ "stable" channel

---

## ✅ สรุป

**วิธีที่ง่ายที่สุด:**
1. `wsl`
2. `sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"`
3. เลือก "stable"

**ใช้เวลา:** ~3-5 นาที

---

**ลองใช้ WSL ดูครับ!** 🚀



