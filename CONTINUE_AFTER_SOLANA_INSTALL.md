# ✅ Solana CLI ติดตั้งสำเร็จแล้ว! - ขั้นตอนต่อไป

## 🎉 สถานะปัจจุบัน

- ✅ WSL Ubuntu พร้อมใช้งาน
- ✅ Solana CLI ติดตั้งสำเร็จ
- ✅ PATH ถูกเพิ่มแล้ว
- ⏳ Rust - ต้องตรวจสอบ
- ⏳ Anchor Framework - ต้องติดตั้ง

---

## ✅ ขั้นตอนที่ 1: ตรวจสอบ Solana CLI

```bash
# ใน WSL terminal
solana --version
```

**ควรเห็น:** `solana-cli 1.18.xx` หรือ version ที่ติดตั้ง

---

## ✅ ขั้นตอนที่ 2: ตรวจสอบ Rust

```bash
# ใน WSL terminal
rustc --version
```

### ถ้ายังไม่มี Rust:

```bash
# ติดตั้ง Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# เลือก default (กด Enter)

# Reload shell
source ~/.bashrc

# ตรวจสอบ
rustc --version
```

**ควรเห็น:** `rustc 1.xx.x`

---

## ✅ ขั้นตอนที่ 3: ติดตั้ง Anchor Framework

```bash
# ใน WSL terminal

# ติดตั้ง Anchor Version Manager (avm)
cargo install --git https://github.com/coral-xyz/anchor avm --locked --force

# รอให้ติดตั้งเสร็จ (~5-10 นาที)

# ติดตั้ง Anchor latest
avm install latest
avm use latest

# ตรวจสอบ
anchor --version
```

**ควรเห็น:** `anchor-cli 0.xx.x`

---

## ✅ ขั้นตอนที่ 4: ตรวจสอบ Tools ทั้งหมด

```bash
# ใน WSL terminal
rustc --version
solana --version
anchor --version
```

**ควรเห็นทั้ง 3 ตัว:**
- `rustc 1.xx.x`
- `solana-cli 1.18.xx`
- `anchor-cli 0.xx.x`

---

## ✅ ขั้นตอนที่ 5: Build Solana Program

### ไปที่โปรเจค:

```bash
# ใน WSL terminal
cd /mnt/c/Users/ADMIN/Downloads/jjdh\ a
```

### Build:

```bash
anchor build
```

**รอให้ build เสร็จ (~5-10 นาที ครั้งแรก)**

---

## ✅ ขั้นตอนที่ 6: ตั้งค่า Solana และ Deploy

### ตั้งค่า devnet:

```bash
# ใน WSL terminal
solana config set --url devnet
solana config get
```

### สร้าง wallet (ถ้ายังไม่มี):

```bash
solana-keygen new
```

**บันทึก seed phrase ไว้ในที่ปลอดภัย!**

### รับ airdrop:

```bash
solana airdrop 2
```

### Deploy program:

```bash
anchor deploy
```

---

## 🎯 สรุปคำสั่งทั้งหมด (รันใน WSL terminal)

```bash
# 1. ตรวจสอบ Solana
solana --version

# 2. ตรวจสอบ Rust
rustc --version

# 3. ติดตั้ง Rust (ถ้ายังไม่มี)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.bashrc

# 4. ติดตั้ง Anchor
cargo install --git https://github.com/coral-xyz/anchor avm --locked --force
avm install latest
avm use latest

# 5. ตรวจสอบทุก tool
rustc --version
solana --version
anchor --version

# 6. Build program
cd /mnt/c/Users/ADMIN/Downloads/jjdh\ a
anchor build

# 7. ตั้งค่าและ deploy
solana config set --url devnet
solana-keygen new
solana airdrop 2
anchor deploy
```

---

## ⏱️ เวลาที่ใช้

- **ตรวจสอบ Solana:** ~10 วินาที ✅ (เสร็จแล้ว)
- **ติดตั้ง Rust:** ~5-10 นาที (ถ้ายังไม่มี)
- **ติดตั้ง Anchor:** ~5-15 นาที
- **Build Program:** ~5-10 นาที (ครั้งแรก)
- **Deploy:** ~2-5 นาที

**รวม:** ~15-40 นาที (ขึ้นอยู่กับว่ามี Rust หรือยัง)

---

## 💡 หมายเหตุ

- **ใช้ WSL terminal** สำหรับคำสั่งทั้งหมด
- **PATH ถูกเพิ่มใน ~/.profile แล้ว** - ใช้ได้ทุกครั้งที่เปิด WSL
- **Build ครั้งแรกจะใช้เวลานาน** เพราะต้อง compile dependencies
- **ถ้า Rust ยังไม่มี** - ต้องติดตั้งก่อน Anchor

---

## 🚀 เริ่มเลย!

**ขั้นตอนแรก: ตรวจสอบ Solana CLI**

```bash
# ใน WSL terminal
solana --version
```

**ถ้าเห็น version แล้ว → ไปขั้นตอนที่ 2: ตรวจสอบ Rust**

---

**พร้อมแล้ว! เริ่มตรวจสอบ Solana CLI ก่อน** 🎉


