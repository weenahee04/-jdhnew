# 🚀 ติดตั้ง Solana CLI - ใช้ WSL (แนะนำที่สุด)

## ⚠️ ปัญหา Windows Installer

Windows installer มีปัญหา:
- ไฟล์ไม่พบใน Temp
- ไม่รองรับ "stable" channel
- ต้องระบุ version ที่ซับซ้อน

---

## ✅ วิธีแก้: ใช้ WSL (ง่ายที่สุด)

### ขั้นตอน:

1. **เปิด WSL Terminal:**
   ```powershell
   wsl
   ```

2. **รันคำสั่งติดตั้ง:**
   ```bash
   sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
   ```

3. **เลือก "stable" เมื่อถูกถาม**

4. **รอให้ติดตั้งเสร็จ** (~2-3 นาที)

5. **ตั้งค่า PATH:**
   ```bash
   echo 'export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc
   ```

6. **ตรวจสอบ:**
   ```bash
   solana --version
   ```

**เสร็จแล้ว!** ✅

---

## 📝 ใช้ Solana CLI จาก PowerShell

**เรียกใช้ผ่าน WSL:**
```powershell
# ตรวจสอบ version
wsl solana --version

# ตั้งค่า devnet
wsl solana config set --url devnet

# รับ SOL ฟรี
wsl solana airdrop 2

# ตรวจสอบ config
wsl solana config get
```

**หรือเข้า WSL ก่อน:**
```powershell
wsl
# แล้วใช้คำสั่ง solana ตามปกติ
solana --version
solana config set --url devnet
solana airdrop 2
```

---

## 🎯 สำหรับ Build Program

**ใช้ Anchor ใน WSL:**
```bash
# ใน WSL
cd /mnt/c/Users/ADMIN/Downloads/jjdh\ a
anchor build
anchor deploy
```

---

## 💡 ทำไมใช้ WSL?

- ✅ ง่ายกว่า - ไม่ต้องระบุ version
- ✅ เสถียรกว่า - ลิงก์ใช้ได้แน่นอน
- ✅ ไม่มีปัญหา format
- ✅ รองรับ "stable" channel
- ✅ ใช้ได้กับ Anchor build/deploy

---

## ✅ สรุป

**วิธีที่ง่ายที่สุด:**
1. `wsl`
2. `sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"`
3. เลือก "stable"
4. ตรวจสอบ: `solana --version`

**ใช้เวลา:** ~3-5 นาที

---

**ลองใช้ WSL ดูครับ!** 🚀



