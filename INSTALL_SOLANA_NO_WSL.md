# 🚀 ติดตั้ง Solana CLI - ไม่ต้องใช้ WSL

## ✅ วิธีติดตั้ง (Windows Installer + Version เฉพาะ)

### ขั้นตอนที่ 1: หา Version ล่าสุด

1. **ไปที่ GitHub Releases:**
   https://github.com/anza-xyz/agave/releases

2. **ดู version ล่าสุด** (เช่น `v1.18.26`)

---

### ขั้นตอนที่ 2: ดาวน์โหลด Installer

1. **ดาวน์โหลด installer:**
   - ไปที่: https://github.com/anza-xyz/agave/releases/latest
   - ดาวน์โหลด: `solana-install-init-x86_64-pc-windows-msvc.exe`
   - หรือใช้ลิงก์: https://release.anza.xyz/stable/solana-install-init-x86_64-pc-windows-msvc.exe

2. **บันทึกไว้ที่ Downloads**

---

### ขั้นตอนที่ 3: ติดตั้ง

1. **เปิด PowerShell:**
   ```powershell
   cd $env:USERPROFILE\Downloads
   ```

2. **รัน installer พร้อม version:**
   ```powershell
   .\solana-install-init-x86_64-pc-windows-msvc.exe --data-dir "$env:USERPROFILE\.local\share\solana\install" v1.18.26
   ```

   **หมายเหตุ:** แทนที่ `v1.18.26` ด้วย version ล่าสุดที่คุณหาได้

3. **รอให้ติดตั้งเสร็จ (~2-3 นาที)**

---

### ขั้นตอนที่ 4: เพิ่ม PATH

1. **เพิ่ม PATH:**
   ```powershell
   $solanaPath = "$env:USERPROFILE\.local\share\solana\install\active_release\bin"
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";$solanaPath", [EnvironmentVariableTarget]::User)
   ```

2. **รีสตาร์ท PowerShell (ปิดแล้วเปิดใหม่)**

---

### ขั้นตอนที่ 5: ตรวจสอบ

```powershell
solana --version
```

**ควรเห็น:** `solana-cli 1.18.26` (หรือ version ที่ติดตั้ง)

---

## 🎯 ตั้งค่า Solana

### ตั้งค่า Network:

```powershell
solana config set --url devnet
```

### ตรวจสอบ Configuration:

```powershell
solana config get
```

### สร้าง Wallet (ถ้ายังไม่มี):

```powershell
solana-keygen new
```

### รับ Airdrop (Devnet):

```powershell
solana airdrop 2
```

---

## ✅ เสร็จแล้ว!

ตอนนี้คุณสามารถใช้ Solana CLI ได้แล้ว! 🎉

---

## 📋 Version ล่าสุด

**Latest Stable:**
- `v1.18.26` - Latest (แนะนำ)

**ดู version ทั้งหมด:**
https://github.com/anza-xyz/agave/releases

---

## 🔧 แก้ไขปัญหา

### ถ้า `solana --version` ไม่ทำงาน:

1. **ตรวจสอบ PATH:**
   ```powershell
   $env:Path -split ';' | Select-String "solana"
   ```

2. **ถ้าไม่มี:**
   - เพิ่ม PATH อีกครั้ง (ขั้นตอนที่ 4)
   - รีสตาร์ท PowerShell

3. **ถ้ายังไม่ได้:**
   - รีสตาร์ทคอมพิวเตอร์

---

## 💡 หมายเหตุ

- **ไม่ต้องใช้ WSL** - วิธีนี้ใช้ Windows installer โดยตรง
- **ต้องระบุ version** - ไม่สามารถใช้ "stable" หรือ "latest" ได้
- **ต้องรีสตาร์ท PowerShell** - หลังเพิ่ม PATH

---

**ลองทำตามขั้นตอนนี้ดู!** 🚀

