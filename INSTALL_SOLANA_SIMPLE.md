# 🚀 ติดตั้ง Solana CLI - วิธีง่ายๆ

## ⚡ วิธีที่ง่ายที่สุด (3 ขั้นตอน)

### 1. ดาวน์โหลด
- เปิด Browser (Chrome, Edge, etc.)
- ไปที่ลิงก์นี้: **https://release.solana.com/stable/solana-install-init-x86_64-pc-windows-msvc.exe**
- คลิกดาวน์โหลด (ไฟล์จะอยู่ในโฟลเดอร์ Downloads)

### 2. รัน
- ไปที่โฟลเดอร์ Downloads
- ดับเบิลคลิกไฟล์ `solana-install-init-x86_64-pc-windows-msvc.exe`
- เลือก **"1"** หรือพิมพ์ **"stable"** แล้วกด Enter
- รอให้ติดตั้งเสร็จ (~2-3 นาที)

### 3. ตรวจสอบ
- **ปิด PowerShell เก่า**
- **เปิด PowerShell ใหม่**
- พิมพ์: `solana --version`
- ควรเห็น: `solana-cli x.x.x` ✅

---

## 🎯 หรือใช้สคริปต์อัตโนมัติ

```powershell
.\install-solana-easy.ps1
```

สคริปต์จะทำทุกอย่างให้อัตโนมัติ!

---

## ❓ ถ้ายังไม่ได้

### ปัญหา: "solana: command not found"

**แก้:**
1. ปิด PowerShell ทั้งหมด
2. เปิด PowerShell ใหม่
3. ลองอีกครั้ง: `solana --version`

**ถ้ายังไม่ได้:**
```powershell
# เพิ่ม PATH manually
$env:PATH += ";$env:USERPROFILE\.local\share\solana\install\active_release\bin"
solana --version
```

---

## ✅ หลังติดตั้งสำเร็จ

```powershell
# สร้าง wallet (ถ้ายังไม่มี)
solana-keygen new

# ตั้งค่า devnet
solana config set --url devnet

# รับ SOL ฟรี
solana airdrop 2

# ตรวจสอบ
solana config get
```

---

## 📝 สรุป

**วิธีง่ายที่สุด:**
1. ดาวน์โหลดด้วย Browser
2. ดับเบิลคลิกเพื่อรัน
3. เลือก "stable"
4. รีสตาร์ท PowerShell
5. ตรวจสอบ: `solana --version`

**ใช้เวลา:** ~5 นาที

---

**พร้อมแล้ว!** 🎉



